/**
 * Placeholder: Lunr index + Lucene-lite parser (docs/SEARCH.md).
 * @param {string} query
 * @param {object[]} documents
 */
export function search(query, documents = []) {
  if (!query || !query.trim()) return documents;
  return documents;
}
